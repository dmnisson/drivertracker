@model IEnumerable<DriverTracker.Models.Analysis>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Analyst)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Driver)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Analyst.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Driver.DriverID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnalysisID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnalysisID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnalysisID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
